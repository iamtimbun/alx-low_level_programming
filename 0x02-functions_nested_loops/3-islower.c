#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
  *_islower - check for small case
  *
  *Return: Always 0.
  */

int _islower(int c)
{
	if(_islower(c) == 0)
	{
		_putchar(1);
	}else{
		_putchar(0);
	}
	return 0;
}
