#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
  *int _islower - check for small case
  *
  *Return: Always 0.
  */

int _islower(int c)
{
	if(_islower(int c) == 0)
	{
		printf(1);
	}else{
		printf(0);
	}
	return 0;
}
