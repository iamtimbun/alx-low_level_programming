#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
  *int _islower - check for small case
  *
  *Return: Always 0.
  */

int _islower(int c)
{
	if(_islower(c) == 0)
	{
		printf("is lower");
	}else{
		printf("not lower");
	}
	return 0;
}
