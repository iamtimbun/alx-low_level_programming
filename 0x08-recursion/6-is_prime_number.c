#include <stdio.h>
#include "main.h"

/**
 * is_prime_number(int n)- prints prime numbers
 * @n: parameter
 * Return: Prime Number
 */

int is_prime_number(int n)
{
	if (n <= 1)
		return (0);
	else
		return (1);
}
